# Core
pydantic-settings
python-dotenv
alembic
SQLAlchemy[asyncio]

# API
fastapi
uvicorn

# Additional libraries
langchain-core
langgraph-sdk

# If you want MySQL or PostgreSQL drivers:
psycopg
# For MySQL:
cryptography
# Async MySQL driver for async SQLAlchemy
asyncmy
aiomysql
# Sync MySQL driver for Alembic migrations (if not already installed)
PyMySQL

# Template

#SMTP
